# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'YoutubeKit-Sample' do
  use_frameworks!

  pod 'YoutubeKit'

    plugin 'cocoapods-keys', {
    :project => "YoutubeKit-Sample",
    :keys => [
      "APIKey",
    ]}

end
